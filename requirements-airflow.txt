# Extra deps baked into the custom Airflow image (docker/airflow/Dockerfile).
# These are the libraries the Phase 3 DAG tasks import at runtime; the stock
# apache/airflow image ships none of them. Baked at build time (NOT via
# _PIP_ADDITIONAL_REQUIREMENTS) so they are not reinstalled on every container
# start. Versions kept in lockstep with requirements.txt.
xgboost==2.1.3
scikit-learn==1.5.2
mlflow==2.19.0
great-expectations==0.18.19
evidently==0.4.40
optuna==4.1.0
joblib==1.4.2
requests==2.32.3
