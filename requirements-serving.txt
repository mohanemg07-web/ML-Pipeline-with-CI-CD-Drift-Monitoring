# Minimal deps for the Render free-tier serving image (512 MB RAM budget).
# No training libs, no dev/test tools, mlflow-skinny instead of full mlflow.
fastapi==0.115.6
uvicorn==0.32.1
pydantic==2.10.4
prometheus-client==0.21.1
# xgboost-cpu: same `import xgboost` package, no nvidia-nccl-cu12 payload.
# Training (requirements.txt) keeps regular xgboost.
xgboost-cpu==2.1.3
scikit-learn==1.5.2
pandas==2.1.4
numpy==1.26.4
mlflow-skinny==2.19.0
python-dotenv==1.0.1
