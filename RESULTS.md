# RESULTS — metric → evidence → provenance

The project rule: **no metric appears anywhere in this repo unless it was read
from a raw results file generated by this pipeline.** This file is that
contract made literal. Every claim links the committed evidence file and states
the command (or recorded process) that produced it. If a number you see in the
README is not in this table, it is a bug — delete it.

## Model quality — [`eval/results/training_metrics.json`](eval/results/training_metrics.json)

Produced by: `python -m src.train --trials 25` (seed 42, Python 3.10.4 local,
local MLflow file store; the champion run was later re-logged to DagsHub by
`python -m src.register_dagshub`).

| Claim | Value in file (JSON path) |
|---|---|
| Champion test ROC-AUC | `0.8527141937856222` (`xgboost.test.roc_auc`) |
| Champion test PR-AUC | `0.6643727169295549` (`xgboost.test.pr_auc`) |
| Baseline LogReg test ROC-AUC | `0.8495150763007905` (`baseline_logreg.test.roc_auc`) |
| Baseline LogReg test PR-AUC | `0.6362008175844294` (`baseline_logreg.test.pr_auc`) |
| Optuna budget | `25` (`xgboost.n_trials`) |
| Dataset size / split | `7043` total; 4929/1057/1057 train/val/test (`dataset.*`) |
| Quality gate | `0.8` (`auc_gate`), `gate_passed: true` |

## Live-service reliability — [`eval/results/load_test.json`](eval/results/load_test.json)

Produced by: ad-hoc Phase 2 scripts (`cold_start.py`, `warm_load.py`) run from a
residential connection in India against the live Render service (Singapore);
the scripts' raw stdout is committed as
[`load_test_raw.txt`](eval/results/load_test_raw.txt). Method parameters
(n=5000, concurrency 8, seeded rng(42), varied realistic payloads) are recorded
inside the JSON itself.

| Claim | Value in file (JSON path) |
|---|---|
| Cold start (n=1, after 20.8 min verified idle) | `32.5` s (`cold_start.latency_s`) |
| Warm requests OK | `5000` of 5000, `"status_counts": {"200": 5000}` |
| 5xx / network errors | `0` (`warm.errors_5xx_or_network`) |
| Throughput | `18.17` req/s (`warm.throughput_rps`) |
| Client-observed latency (RTT included) | P50 `421.7` / P95 `600.9` / P99 `696.4` ms (`warm.latency_ms`) |

## Drift detection — [`eval/results/drift_report.json`](eval/results/drift_report.json)

Produced by: the `churn_drift_retrain` Airflow DAG's drift-check task
(`src/drift.py`, Evidently PSI), on a batch generated by
`python -m src.simulate_drift --n 1500 --seed 7 --severity 1.0` (shift
parameters recorded in
[`data/production_batch.meta.json`](data/production_batch.meta.json); HTML
report at [`monitoring/drift_report.html`](monitoring/drift_report.html)).

| Claim | Value in file (JSON path) |
|---|---|
| PSI threshold / min drifted features to trigger | `0.2` / `2` (`threshold`, `min_features`) |
| Drifted features (5 of 19) | tenure `2.73073`, MonthlyCharges `1.703045`, TotalCharges `0.436431`, PaymentMethod `0.369129`, InternetService `0.353` (`psi_by_feature`) |
| Decision | `detected: true` |

## Retrain → promote → redeploy loop — [`eval/results/retrain_loop.json`](eval/results/retrain_loop.json)

Produced by: the full `churn_drift_retrain` DAG run (Airflow in Docker Compose,
2026-07-13), which retrained on the drifted batch, passed the promotion gate,
promoted v2 in the DagsHub registry, fired the Render deploy hook, and polled
the live `/health` until the new identity was served.

| Claim | Value in file (JSON path) |
|---|---|
| Stale champion ROC-AUC on drifted eval slice | `0.851536` (`auc_on_drifted_eval.stale_champion`) |
| Retrained ROC-AUC on drifted eval slice | `0.85662` (`auc_on_drifted_eval.retrained`) |
| Recovery delta | `+0.005084` (`auc_on_drifted_eval.recovery_delta`) |
| Promotion gate | `PASS: new_auc=0.8566 >= stale_auc=0.8515 - tol=0.02`; smoke test `PASS` |
| Detection → retrain done | `59.6` s (`timings_s.detection_to_retrain_done_s`) |
| Retrain done → hook fired | `60.0` s (`timings_s.retrain_done_to_hook_fired_s`) |
| Hook fired → verified live | `46.9` s, 1 poll (`timings_s.hook_fired_to_verified_live_s`) |
| **Total detection → new model live** | **`166.5` s** (`timings_s.total_detection_to_live_s`) |
| Live identity after redeploy | version `2`, run `4f476fb4635e4c19a0a8cec33656e57b` (`redeploy.last_health`) |

## CI/CD — GitHub Actions ([`.github/workflows/ci.yml`](.github/workflows/ci.yml))

Produced by: the CI pipeline itself; numbers read from the CI log of
[run 29259002268](https://github.com/mohanemg07-web/ML-Pipeline-with-CI-CD-Drift-Monitoring/actions/runs/29259002268)
(first green run, commit `0709754`) and recorded in [`NOTES.md`](NOTES.md)
(Checkpoint 4). Coverage is NOT re-stated from a local run — it is the number
the CI job printed.

| Claim | Source |
|---|---|
| Test coverage 63.2 % (753 stmts, 277 miss, src/ + serving/, Python 3.11) | CI log line `Measured test coverage: 63.2%`, run 29259002268 |
| Quality gate in CI | CI log line `Quality gate PASSED: champion test ROC-AUC 0.8527 >= gate 0.80` |
| First green run wall time | 3m17s (checks 2m0s + build-and-deploy 1m9s), run 29259002268 |
| Test count | 56 at Checkpoint 4; 58 after Phase 5 instrumentation tests (both in NOTES.md checkpoints) |

## Monitoring & alerting — [`eval/results/monitoring_verification.json`](eval/results/monitoring_verification.json)

Produced by: scripted verification against the public APIs (Prometheus
`/api/v1/*` at localhost:9090, Alertmanager `/api/v2/*` at localhost:9093,
Grafana HTTP API, the live Render endpoint, and `docker compose logs
alert-webhook`); each check in the JSON records its timestamp, the exact query,
and the returned value. Stage 1 ran pre-deploy, stage 2 after commit `10b10c8`
deployed via [run 29262816775](https://github.com/mohanemg07-web/ML-Pipeline-with-CI-CD-Drift-Monitoring/actions/runs/29262816775).

| Claim | Value in file (JSON path) |
|---|---|
| Live target UP in Prometheus | `health: "up"`, warm scrape ~`0.148` s (stage1 `prometheus_target_up`) |
| Traffic burst integrity | 40/40 HTTP 200; counter delta exactly `40.0` (stage1 checks) |
| In-process scoring latency | P50 `0.0035` s / P99 `0.0098` s under steady ~2 req/s (stage1 `p99_scoring_latency_visible`) |
| Live model identity on the metrics path | `churn_model_info{model_version="2", model_run_id="4f476fb4..."}` matching `/health` (stage2 `live_model_identity_v2`) |
| Alert demonstration (`ChurnClientErrorSpike`) | 1062 invalid payloads, all `422`; 4xx share at firing `0.6823`; pending `18:07:15Z` → firing `18:09:15Z` → webhook `18:09:33Z` → resolved `18:12:00Z` → resolved webhook `18:12:32Z` (stage2 checks) |
| 5xx rule selectivity | `ChurnHigh5xxRate` stayed `inactive` through the 422 burst — by design (stage2 `high_5xx_rate_rule_not_fired`) |

## Dashboard screenshots — [`monitoring/dashboard.png`](monitoring/dashboard.png), [`monitoring/dashboard_error_spike.png`](monitoring/dashboard_error_spike.png)

Captured manually from the file-provisioned Grafana dashboard while steady live
traffic ran (2026-07-13 ~23:49–23:59 IST): model identity v2, endpoint UP,
0.00 % 5xx, and the 62.5 % 4xx tooltip during the alert demonstration window.
