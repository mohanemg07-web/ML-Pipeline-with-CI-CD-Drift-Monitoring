# Full dev/training/orchestration environment (NOT the Render serving image —
# see requirements-serving.txt for the slim serving deps).
# Python 3.11. All versions pinned; verified at install time in Phase 1.

# Core data/ML
pandas==2.1.4
numpy==1.26.4
scikit-learn==1.5.2
xgboost==2.1.3
optuna==4.1.0
matplotlib==3.9.3
joblib==1.4.2

# Experiment tracking / registry (DagsHub-hosted MLflow)
mlflow==2.19.0

# Data validation & drift
great-expectations==0.18.19
evidently==0.4.40

# Serving (also pinned separately in requirements-serving.txt)
fastapi==0.115.6
uvicorn==0.32.1
prometheus-client==0.21.1
pydantic==2.10.4

# Testing & lint
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
ruff==0.8.4

# Utilities
requests==2.32.3
python-dotenv==1.0.1
